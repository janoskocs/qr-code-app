import "./CustomPallet.scss";

const CustomPallet = () => {

    return (
        <>  
        <label className="custom-pallet">
            Enter a custom pallet:
            <input  type="text"/>
        </label>
        </>
    );
}

export default CustomPallet;